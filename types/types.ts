export type userProfileType = {
  name: string;
  email: string;
  uid: string;
};

export type EventType = {
  description: string;
  id: number;
  name: string;
  photoUrl: string;
  time: string;
  venue: string;
};
